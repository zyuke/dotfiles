{
    "layer": "top", // Waybar at top layer
    "position": "left", // Waybar position (top|bottom|left|right)
    "width": 26, // Waybar width
    "spacing": 0, // Gaps between modules (4px)
    "margin-top": 0,
    "margin-left": 0,
    "margin-bottom": 0,
    "ipc": true,

    // Choose the order of the modules
    "modules-left": ["tray", "battery", "pulseaudio/slider", "pulseaudio#audio", "backlight", "network", "cpu", "memory", "temperature", "power-profiles-daemon"],
    "modules-center": ["niri/workspaces"],
    "modules-right": ["clock"],

    // Modules configuration
    "niri/workspaces": {
        "all-outputs": false,
        "format": "{icon}",

        "format-icons": {
            "active": "",
            "default": ""
        },
        "on-click": "activate"
    },

    "tray": {
        "icon-size": 14,
        "spacing": 10,
    },

    "pulseaudio#audio": {
        "format": "{icon}",
        "format-muted": "󰟎",
        "format-icons": {
            "headphone": "󰋋",
            "headset": "󰋋",
            "default": ["󰋋"]
        },
        "on-click": "pactl set-sink-mute @DEFAULT_SINK@ toggle",
        "on-scroll-up": "pactl set-sink-volume @DEFAULT_SINK@ +1%",
        "on-scroll-down": "pactl set-sink-volume @DEFAULT_SINK@ -1%",
        "tooltip": true,
        "tooltip-format": "{desc}\nVolume: {volume}%",
    },

    "pulseaudio/slider": {
        "min": 0,
        "max": 100,
        "orientation": "vertical"
    },

    "backlight": {
        "format": "{icon}",
	"format-icons": ["󰃟"],
	"tooltip": true,
        "tooltip-format": "Brightness: {percent}%",
    },

    "network": {
        // "interface": "wlp2*", // (Optional) To force the use of this interface
        "format-wifi": "{icon}",
        "format-icons": ["󰤟", "󰤢", "󰤥", "󰤨"],
        "format-ethernet": "",
        "format-disconnected": "⚠",
        "tooltip-format": "{essid}\n{ifname} via {gwaddr}",
        "tooltip-format-ethernet": "{ifname} ",
        "tooltip-format-disconnected": "Disconnected",
    },

    "cpu": {
	"interval": 5,
        "format": "",
        "tooltip": true
    },

    "memory": {
	"interval": 5,
        "format": "",
        "tooltip-format": "{used:0.1f}G/{total:0.1f}G"
    },

    "temperature": {
        "hwmon-path-abs": "/sys/devices/pci0000:00/0000:00:18.3/hwmon/",
	"input-filename": "temp1_input",
        "critical-threshold": 80,
        "format": "{icon}",
        "format-icons": [""],
	"tooltip-format": "CPU: {temperatureC}°C"
    },

    "battery": {
        "states": {
            "warning": 30,
            "critical": 15
        },
        "format": "{icon}",
        "format-charging": "󰂄",
        "format-plugged": "󱘖",
        "format-icons": ["󰁺", "󰁻", "󰁼", "󰁽", "󰁾", "󰁿", "󰂀", "󰂁", "󰂂", "󰁹"],
        "on-click": "",
        "tooltip-format": "Battery: {capacity}%"
    },

    "power-profiles-daemon": {
        "format": "{icon}",
        "tooltip": true,
        "tooltip-format": "Power profile: {profile}",
        "format-icons": {
            "performance": "",
            "balanced": "󰗑",
            "power-saver": ""
        }
    },

    "clock": {
        "interval": 1,
        "format": "<span font='18px'>{:%I\n%M\n%p}</span>",
        "format-alt": "<span font='18px'>{:%m\n%d\n%y}</span>",
        "tooltip": true,
        "tooltip-format": "<span font='14px'>{calendar}</span>",
        "calendar": {
            "mode": "year",
            "mode-mon-col": 3,
            "format": {
                "today": "<span color='#0dbc79'>{}</span>"
            }
        }
    },
}
